PUBLIC_SITE_URL=https://skovbyesexologi.com
PUBLIC_ALLOW_INDEXING=true
PUBLIC_GITHUB_REPO=den-frie-vilje/skovbyesexologi.com

# Build marker — populated by CI at build time
# (see .github/workflows/build-and-notify.yml).
# Empty in local dev builds; the footer's build-marker
# element just renders nothing when empty.
PUBLIC_GIT_SHA=
PUBLIC_BUILD_TIME=
